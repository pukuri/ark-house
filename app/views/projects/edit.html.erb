<% content_for :title, "Editing project" %>

<h1>Editing project</h1>

<%= render partial: "form", locals: { project: @project, members: @members } %>

<br>

<div>
  <%= link_to "Show this project", @project %> |
  <%= link_to "Back to projects", projects_path %>
</div>
